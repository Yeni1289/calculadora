services:
  - type: web
    name: calculadora
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn calculadora.wsgi:application
    pythonVersion: 3.13

